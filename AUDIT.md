# Аудит сайту ARMADERO — 2026-09-23

Повний аудит: код, SEO, продуктивність, доступність, безпека, тексти.

**Статус:** усі 36 пунктів пройдені разом з власницею сайту.

- ✅ **Виправлено** — застосовано зміну, закомічено й запушено.
- ⏸ **Відкладено** — свідомо не зараз, повернутись пізніше.
- 🚫 **Пропущено назавжди** — власниця вирішила не чіпати, не пропонувати знову.

---

## 🔴 Критично

1. ✅ **RCE-вразливість у Next.js 16.3.2.** `npm audit`: critical — unauthenticated RCE на Windows-хостингу, і RCE через Image Optimization API з AVIF-файлами (GHSA-p293-qw3h-jr36, GHSA-2xp9-vwfh-vxw4). Також `sharp <0.35.4` — high, вразливості в libheif. **Виправлено:** оновлено Next.js до 16.3.6, `npm audit` — 0 вразливостей.

2. ✅ **Немає жодного `<h1>` на реальній сторінці.** Єдиний `<h1>` був у `IntroOverlay.tsx`, зникав назавжди після першого візиту. **Виправлено:** заголовок Hero промотано до `<h1>`, intro-логотип позначено `aria-hidden`.

3. ✅ **Немає `robots.txt` і `sitemap.xml`.** **Виправлено:** додано `src/app/robots.ts` і `src/app/sitemap.ts` (+ `metadataBase`).

4. ✅ **Немає Open Graph / Twitter Card метаданих.** **Виправлено:** додано `openGraph`/`twitter` в `layout.tsx`, згенеровано `og-image.jpg` (1200×630, 132КБ) з hero-фото.

5. ✅ **21+ МБ картинка на hero-екрані**, `public/` ≈76 МБ неоптимізованих PNG. **Виправлено:** hero сконвертовано в WebP (21.7МБ → 1МБ), решта фото сайту (~52МБ → ~5.5МБ), plus локально захостили зовнішні stock-фото (п. 24).

6. ✅ **Текст, введений у форму контактів, майже невидимий.** **Виправлено:** введений текст тепер темний (`text-ink`), плейсхолдер лишився світлим.

7. ⏸ **Секція контактів повністю нерезивна.** Жодного `sm:`/`md:`/`lg:` класу, фіксовані розміри. **Відкладено** — власниця адаптуватиме сайт під різні розміри екрана окремим заходом пізніше.

8. ⏸ **На мобільних немає навігації взагалі.** Nav/телефон/перемикач мови сховані на мобільному без гамбургер-меню. **Відкладено** — разом з п. 7, як частина майбутньої адаптивної верстки.

9. ✅ **Немає rate limiting на `/api/contact`.** **Виправлено:** додано `@upstash/ratelimit` (5 запитів/10хв/IP), налаштовано Upstash Redis.

10. ✅ **Фейковий номер телефону в проді.** **Виправлено:** номер у хедері замінено на реальний (той самий, що у футері).

11. ✅ **Фейкові відгуки видаються за реальні.** **Виправлено:** прибрано слово "Реальні" з підпису над відгуками.

12. ✅ **Розбіжність у гарантійних умовах.** Meta-опис казав "5 років" без застережень. **Виправлено:** "гарантія до 5 років".

13. ✅ **Форма контактів містила непереладений англійський текст** і мертві посилання на неіснуючі "terms"/"privacy policy". **Виправлено:** створено реальні сторінки `/privacy` і `/terms` (на основі укр. законодавства), текст форми перекладено, посилання в футері й формі ведуть на них.

14. ⏸ **Перемикач мов UA/EN нефункціональний.** **Відкладено** — власниця сказала "не зараз", можливо повернеться, коли з'явиться реальний переклад.

---

## 🟡 Бажано

15. 🚫 **Майже весь сайт — client components.** Переглянуто всі 18 файлів — усі справді потребують `"use client"` через GSAP/framer-motion анімації (осмислений архітектурний вибір, задокументований у CLAUDE.md). **Пропущено назавжди** — не проблема, а наслідок дизайну.

16. ✅ **FAQ-акордеон без ARIA.** **Виправлено:** додано `aria-expanded`/`aria-controls`/`role="region"`/`aria-labelledby`.

17. ✅ **Немає підтримки `prefers-reduced-motion`** для framer-motion-компонентів. **Виправлено:** весь сайт обгорнуто в `<MotionConfig reducedMotion="user">`.

18. ✅ **Валідація полів на бекенді лише presence-check.** **Виправлено:** додано regex-валідацію email і телефону в `/api/contact`.

19. ✅ **Форма newsletter у футері без honeypot.** **Виправлено:** додано той самий honeypot-патерн, що в головній формі.

20. ✅ **Помилка відправки newsletter замовчувалась.** **Виправлено:** додано повідомлення про помилку користувачу.

21. ✅ **CTA "Отримати візуалізацію" не відповідав функціоналу форми.** **Виправлено:** "Надіслати заявку" / "Залишити заявку" — тексти тепер відповідають дії.

22. 🚫 **Соцмережі у футері ведуть на загальні домени** (instagram.com, facebook.com замість реальних акаунтів). **Пропущено назавжди** — реальних профілів для вигаданого бренду не існує, вигадувати URL не варто.

23. ✅ **Усі футер-посилання були заглушками** ("Про нас", "Політика конфіденційності", "Умови використання"). **Виправлено:** разом з п. 13 — реальні сторінки/якір.

24. ✅ **Ремоут-зображення без контролю на проді** (images.unsplash.com, randomuser.me). **Виправлено:** усі 7 фото завантажено локально, `remotePatterns` прибрано з `next.config.ts`.

25. ✅ **Дублювання inline-кольорів замість design-токенів.** **Виправлено:** усі статичні використання `#362F2B`/`#AF957C`/`#CAC4BF`/`#F6F6F6` замінено на токени (додано нові `--color-brown-200`/`--color-brown-100`). Три місця всередині `framer-motion`'s `animate={{}}` і один alpha-градієнт лишені як є (ризик зламати анімацію).

26. ✅ **Непослідовна ієрархія заголовків.** **Виправлено:** `ProcessFinaleSection.tsx` — єдиний заголовок секції промотано з `h3` до `h2`. `Footer.tsx` виявився коректним (h2 вже передує h3).

27. 🚫 **Термінологічна неузгодженість у футері** ("Newsletter" англ. vs "розсилка" укр.; "Website design by Zalutska" англійською). **Пропущено назавжди** — власниця вирішила залишити як є.

28. 🚫 **Пункти "Острівні"/"Модулі" без зображення в CreateForYouSection.** **Пропущено назавжди** — з'ясувалось, що це свідомий дизайн, фото там не потрібні.

29. ✅ **Порожній `alt=""` на потенційно змістовних зображеннях.** Перевірено всі три: Hero/CatalogSection — справді декоративні, `alt=""` коректний. **Виправлено:** `PortfolioSection.tsx` (peek-прев'ю наступного слайду) — додано описовий alt, узгоджений з поточним слайдом.

---

## ⚪ Дрібниці

30. ✅ **`REPOSITIONING.md` застарілий.** **Виправлено:** оновлено рядок про `images.material` (більше не unsplash-заглушка).

31. ✅ **Невикористана змінна `headerHeight`.** **Виправлено:** видалено, ESLint тепер 0 попереджень.

32. ✅ **Внутрішні ідентифікатори зі старою назвою бренду "vellaro".** **Виправлено:** перейменовано на "armadero-*" в `src/lib/intro.ts` і `layout.tsx`.

33. ✅ **Немає `manifest.json` / кількох розмірів іконок для PWA.** **Виправлено:** додано `manifest.ts` + іконки 192×192/512×512.

34. ✅ **Змішані EOL (LF→CRLF warnings у git).** **Виправлено:** додано `.gitattributes` (`eol=lf`), попередження зникли.

35. 🚫 **Слоган «Світло. Рух. Комфорт.» ніде не пояснюється.** **Пропущено назавжди** — звичний прийом у преміум-маркетингу, власниця вирішила залишити.

36. 🚫 **Капіталізація бренд-нейму «Armadero»/«ARMADERO» відрізняється між секціями.** **Пропущено назавжди** — стилістичний вибір, не помилка.

---

## Джерела перевірки
- `npx tsc --noEmit` — без помилок після кожної зміни.
- `npm run build` — успішний production build після кожної зміни.
- `npm audit` — 0 вразливостей (після п. 1).
- Ручна перевірка в браузері (Claude in Chrome) для критичних UI-змін.
